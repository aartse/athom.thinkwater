import Homey from 'homey'
import UserDeviceResponseInterface from "../types/api/UserDeviceResponseInterface"

export default class ThinkwaterDevice extends Homey.Device {
    updateByUserDevice(userDevice: UserDeviceResponseInterface): void {

    }
}